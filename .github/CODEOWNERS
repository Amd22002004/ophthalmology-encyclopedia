# CODEOWNERS — критические зоны проекта
#
# ┌───────────────────────────────────────────────────────────────────────────┐
# │ ШАБЛОН — ПРАВИЛА ПОКА ОТКЛЮЧЕНЫ (закомментированы).                       │
# │                                                                           │
# │ Чтобы включить, после создания GitHub-репозитория:                        │
# │   1. Заменить <GITHUB-LOGIN> на свой логин (@username) или команду        │
# │      (@org/team). Владелец обязан иметь доступ на запись к репозиторию.   │
# │   2. Снять комментарий (#) со строк правил ниже.                          │
# │                                                                           │
# │ Правила намеренно закомментированы: CODEOWNERS с несуществующим           │
# │ владельцем GitHub считает ошибкой и не применяет ни одно правило.         │
# └───────────────────────────────────────────────────────────────────────────┘
#
# Владелец автоматически назначается ревьюером PR, затрагивающего эти пути.
# Чтобы правило блокировало merge, включите в настройках репозитория:
#   Settings → Branches → Branch protection rules →
#     ☑ Require a pull request before merging
#     ☑ Require review from Code Owners

# ─────────────────────────────────────────────────────────────────────────────
# По умолчанию
# ─────────────────────────────────────────────────────────────────────────────
# *                           <GITHUB-LOGIN>

# ─────────────────────────────────────────────────────────────────────────────
# 🔒 КРИТИЧЕСКАЯ ЗОНА: модель данных
# Заморожена (v1). Изменения — только после архитектурного решения.
# См. docs/architecture/CHANGELOG.md
# ─────────────────────────────────────────────────────────────────────────────
# /prisma/                    <GITHUB-LOGIN>
# /prisma/schema.prisma       <GITHUB-LOGIN>
# /prisma/seed.ts             <GITHUB-LOGIN>

# Каталог миграций. Сейчас отсутствует (схема применяется через db push).
# Правило задано заранее: если миграции появятся — это критическая зона.
# /migrations/                <GITHUB-LOGIN>
# /prisma/migrations/         <GITHUB-LOGIN>

# ─────────────────────────────────────────────────────────────────────────────
# 🔒 КРИТИЧЕСКАЯ ЗОНА: архитектурная документация
# Источник истины проекта наравне со schema.prisma.
# ─────────────────────────────────────────────────────────────────────────────
# /docs/architecture/         <GITHUB-LOGIN>

# ─────────────────────────────────────────────────────────────────────────────
# 🔒 КРИТИЧЕСКАЯ ЗОНА: доступ к данным
# Единая точка запросов к БД; здесь задаётся семантика связей графа.
# ─────────────────────────────────────────────────────────────────────────────
# /src/lib/loaders.ts         <GITHUB-LOGIN>

# ─────────────────────────────────────────────────────────────────────────────
# Инфраструктура и правила разработки
# ─────────────────────────────────────────────────────────────────────────────
# /.github/                   <GITHUB-LOGIN>
# /ecosystem.config.js        <GITHUB-LOGIN>
# /next.config.ts             <GITHUB-LOGIN>
# /prisma.config.ts           <GITHUB-LOGIN>
