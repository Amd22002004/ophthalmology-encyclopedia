# CHANGELOG архитектуры

> Журнал **архитектурных** изменений проекта. Не список git-коммитов.
> Фиксируются только решения, влияющие на модель данных и граф знаний.
>
> Текущее состояние и план — см. [`ROADMAP.md`](./ROADMAP.md).
> Устройство архитектуры — см. [`overview.md`](./overview.md).

---

## v1.5 — 2026-08-13

**Статус: независимый контроль выделен в отдельную ненормативную предметную
область; schema, corpus, publication gates, loaders, search и UI реализованы и
локально проверены через аддитивный diff, двойной seed, build и desktop/mobile.
Состояние production этой записью не подтверждается.**

Причина изменения: локальный «Бланк наблюдения условий осуществления услуг» нельзя
хранить как закон или использовать как автоматическое доказательство нарушения.
Нужна отдельная цепочка «методика → критерий → check question → evidence», которая
может ссылаться на точную норму, но не подменяет её.

### Добавлены сущности и связи

- `IndependentControlMethodology` и `IndependentControlSource` владеют
  библиографией, юридическим статусом рабочего инструмента и отдельным gate прав на
  локальный файл;
- `IndependentControlCriterion` хранит проверяемый факт, вопрос, первичный
  документ, достаточность доказательств, применимость и допустимые статусы;
- `IndependentControlCriterionNorm` направленно связывает критерий с существующим
  `RegulatoryCheck` и точной редакцией, не создавая обратную витрину на
  `/regulations`;
- `InvestigationIndependentControlAssessment` применяет критерий к расследованию,
  optional опубликованной clinic-связи и периоду;
- `InvestigationIndependentControlEvidence` связывает оценку с документом того же
  расследования и ролью `SUPPORTS`, `REFUTES` или `CONTEXT`.

### Корпус и доказательные границы

- извлечены все 80 строк локального бланка; отдельная 81-я запись является formal
  prerequisite применимости официальной НОК и не выдаётся за строку источника;
- локальный DOC классифицирован как ненормативный рабочий инструмент; он не
  получает публичный URL до проверки прав, а public DTO не раскрывает внутреннее
  имя файла и редакционное примечание о правах;
- ненормативные критерии не допускают `CONFIRMED`, `LIKELY_NON_COMPLIANCE` или
  `COMPLIANT`; подтверждающий вывод или вывод о соответствии требует
  `DIRECT_NORM`, применённую edition-bound связь и достаточное первичное
  доказательство;
- для «Глазцентра-Тюмень» 80 исходных оценок остаются внутренними
  `REQUIRES_VERIFICATION`; публична только нейтральная formal-scope карточка с тем
  же статусом и документом роли `CONTEXT`;
- `/independent-control` отделён от `/regulations`, а в расследовании добавлен
  отдельный блок «Проверка по критериям независимой оценки»;
- механизм не создаёт, не обновляет и не удаляет `ClinicOnEquipment`.

Локальная проверка зафиксировала 1 методику, 2 источника, 81 критерий (80 строк
источника + prerequisite) и 81 оценку (80 внутренних + 1 публичная). После двух
seed итоговый SQL diff пуст, а состав из трёх `ClinicOnEquipment` и его контрольная
сумма не изменились.

Каноническая семантика: [`independent-control.md`](./independent-control.md) и
[`regulations.md`](./regulations.md).

---

## v1.4.1 — 2026-08-13

**Статус: редакционные блокеры корпуса А. И. Островерхова сняты после прямого
подтверждения пользователя; девять карточек допущены к локальной публикации.**

- каноническое отчество закреплено как «Иванович», прежний identity blocker снят;
- для всех девяти работ заполнены `evidenceValidatedAt`, `publishedAt` и
  публикационные флаги;
- семь журнальных PDF скопированы в публичный каталог без изменения содержимого;
- полный текст диссертации и автореферат открыты по существующим стабильным URL;
- пользовательское подтверждение распространения записано в `rightsVerifiedAt`,
  `rightsBasis = USER_CONFIRMED_PERMISSION` и `rightsNote` без приписывания
  разрешения автору или издателю; CC BY 4.0 одной статьи
  сохранена как более точное основание `OPEN_LICENSE`;
- статья 2018 года остаётся безопасной `BIBLIOGRAPHIC_ONLY`-карточкой без PDF и
  реконструированных методов, результатов или выводов;
- снят proxy-embargo с двух разрешённых файлов, защита `/admin` не изменена.

Production-деплой в изменение не входит.

---

## v1.4 — 2026-08-12

**Статус: архитектура безопасной публикации `ScientificWork` и девять corpus-
черновиков подготовлены; синхронизация БД и release audit не завершены, новые
работы не разрешены к публичному показу.**

Причина изменения: прежний контракт считал достаточным наличие `slug` и не разделял
полноту найденного источника, научный тип материала, доказательную валидацию и право
сайта хранить локальную копию PDF или изображения. Это позволяло черновику попасть
в каталог или обратную связь раньше редакционного решения.

### Аддитивное расширение `ScientificWork`

- добавлены полные библиографические реквизиты: упорядоченные `authors`,
  `bibliography`, `journal`, `volume`, `issue`, `pages`, уникальный `doi`,
  `sourcePageUrl` и `sourcePdfUrl`;
- `ScientificWorkSourceStatus` разделяет `FULL_TEXT`, `EXTRACTED_PAGES`,
  `SCANNED_PAGES` и `BIBLIOGRAPHIC_ONLY`;
- `ScientificWorkContentKind` разделяет оригинальное исследование, клинический
  случай, обзор, диссертацию и прочий материал, не смешивая шаблоны разделов;
- добавлены `topic`, отдельные `conclusions`, SEO-проекции и publication-поля
  `isPublished`, `publishedAt`, `evidenceValidatedAt`, `publicationBlockReason`;
- локальные `pdfUrl`, `abstractUrl` и `images` получили независимый gate прав через
  `rightsVerifiedAt` и `rightsNote`.

### Единый publication gate

Публичность больше не выводится из `slug`. Единый контракт требует стабильный slug,
явный флаг публикации, наступивший `publishedAt`, evidence-validation и соблюдение
файловых инвариантов. Prisma-проекции используют общий фильтр; raw SQL-поиск
воспроизводит те же условия. Контракт охватывает detail, каталог, блоки врача,
обратные связи, связанные работы, поиск, счётчики и sitemap.

Для `BIBLIOGRAPHIC_ONLY` запрещены локальные PDF/автореферат/изображения и содержательные
массивы `novelty`, `practicalValue`, `results`, `conclusions`. Карточка может
публиковать только проверенные библиографические сведения и явную границу отсутствия
полного текста; исследовательские блоки и PDF-кнопка скрыты.

Внешние `sourcePageUrl` / `sourcePdfUrl` отделены от локальных файлов: ссылка на
источник у издателя не является разрешением на републикацию файла.

### Корпус А. И. Островерхова

Подготовлены девять записей: шесть `FULL_TEXT`, одна `EXTRACTED_PAGES`, одна
`SCANNED_PAGES` и одна `BIBLIOGRAPHIC_ONLY`. Все девять сохранены как непубличные
черновики без локальных ассетов и без дат evidence/publication. Для статьи о
кератоконусе у детей отдельно зафиксирована CC BY 4.0; она не снимает identity и
evidence gates и не открывает карточку автоматически.

Статья 2018 года не содержит реконструированных методов, результатов и выводов.
Полный текст диссертации 2023 года и автореферат обнаружены среди legacy-файлов,
прочитаны и подтверждают «Александр Иванович»; карточка дополнена только
проверяемыми сведениями из этих документов. До проверки provenance и прав прямые
URL обоих файлов закрыты `404`, сами редакционные файлы сохранены. Публикацию
корпуса блокируют редакционное подтверждение исправления отчества и отдельная
проверка прав на PDF/изображения.

Связи графа не менялись: заболевания, процедуры и оборудование остаются прямыми
проверяемыми связями, а клиники — вычисляемыми «Клиниками автора».

Канонический контракт: [`publications.md`](./publications.md).

---

## v1.3 — 2026-08-12

**Статус: архитектурное решение зафиксировано; реализация проходит локальную
стабилизацию и не подтверждает синхронизацию БД или готовность публикации.**

`Regulation` расширен реквизитами, периодом, правовым и публикационным статусом.
Добавлены редакции, официальные источники, темы, направленные связи актов,
атомарные положения, проверочные вопросы и строки стандартов оснащения.

Для расследований добавлены конкретные экземпляры оборудования, применение нормы
к факту и периоду, роли доказательств, первичность и проверка происхождения,
ограниченные сигналы и воспроизводимые реестровые проверки. Пять статусов
фиксированы, а сильный вывод запрещён без применимой редакции, поиска обеих версий
и достаточного первичного доказательства.

Зафиксированы следующие инварианты:

- `/regulations` остаётся нейтральным: физическая обратная связь нужна для аудита,
  но публично показывается только `Investigation -> Regulation`;
- `RegulationSource` является SSOT официальных источников; scalar URL в
  `Regulation` / `RegulationEdition` — контролируемые проекции или legacy;
- assessment, instance, document, timeline event, registry check и snapshot не
  могут смешиваться между расследованиями; семантические проверки, не покрытые FK,
  остаются обязанностью write/publication audit;
- evidence-validation выполняется по публичным узлам: Investigation, section,
  timeline, document, assessment и связи расследования с clinic/equipment/disease/
  procedure имеют собственную отметку; instance проверяется через evidence-связь
  идентификации, registry check — через snapshot-документ;
- применимость редакции требует `verifiedAt`; ретроспективное использование до
  этой даты разрешается только при явном `historicalUseAllowed = true`;
- первичная роль доказательства должна совпадать с итоговым статусом: `SUPPORTS`
  для подтверждающего вывода и `REFUTES` для отрицательного/соответствующего;
- `ClinicOnEquipment` не выводится из расследования. Seed ALLEGRETTO не создаёт и
  не обновляет эту связь, а существующая строка БД, если она есть, не удаляется
  автоматически.

До завершения этапа остаются сверка schema с локальной БД, аддитивного diff,
двойного seed, publication filters, sitemap и существующего ALLEGRETTO-материала.
Каноническая семантика и критерии готовности: [`regulations.md`](./regulations.md).

---

## v1.2 — 2026-08-07

**Статус: аддитивный приватный реестр обращений Ассоциации.**

Добавлены `Appeal`, `AppealAttachment`, `AppealNote`, `AppealStatusHistory`,
`AppealNotification` и `AppealConsentTemplate`, а также статусы обработки и доставки.
Существующие энциклопедические сущности и связи не удалялись и не переименовывались.

`Investigation 1:N Appeal` и nullable `Clinic 1:N Appeal` используются только для
операционной классификации. Сведения о враче и оборудовании со слов заявителя не
создают связей Knowledge Graph. Вложения хранятся вне `public/`, выдаются только
после admin-авторизации и имеют SHA-256.

Появились `/appeal`, `POST /api/appeals`, закрытый реестр `/admin/appeals`, история
статусов, заметки, фильтры, CSV/ZIP-экспорт, email-outbox и версионируемая конфигурация
согласия. Каноническое описание: [`appeals.md`](./appeals.md).

### Канонический публичный сценарий

`/appeal` закреплён как единственная форма любых обращений. Прежний `/contact`
удалён как самостоятельная страница и сохранён только как постоянный 308 redirect;
раздел «Вопросы» ограничен ролью FAQ. Расследования используют основной, плавающий
и итоговый CTA как три состояния одного маршрута, а не как отдельные формы.

## v1.1 — 2026-08-06

**Статус: аддитивное расширение графа научных работ.**

Причина: в клинической статье врача оборудование указано прямо в методике лечения.
Косвенный вывод через процедуру был бы недостаточно точным, поэтому добавлена
прямая связь `ScientificWork ↔ Equipment`.

### Изменения модели

- Добавлена join-таблица `ScientificWorkOnEquipment`.
- В `ScientificWork` добавлено поле `images String[]` для иллюстраций из первичного
  документа научной работы.
- Существующие поля и таблицы не удалялись и не переименовывались.

### Изменения графа

- `ScientificWork ↔ Equipment` разрешена только при прямом упоминании конкретной
  модели или инструмента в первичном тексте работы.
- Запрещён косвенный вывод оборудования научной работы через врача, клинику или
  процедуру.
- Для внешних исследований аппаратов без врача-автора по-прежнему требуется будущая
  сущность `ClinicalStudy`, а не переиспользование `ScientificWork`.

### Изменения отображения

- Страница научной работы показывает оборудование, иллюстрации из документа и
  связанные научные работы.
- Страница оборудования показывает научные работы, где это оборудование прямо
  указано.
- Sitemap теперь включает `ScientificWork` со стабильным `slug`, а не только
  редакционные `Publication`.

---

## v1 — 2026-07-15

**Статус: модель данных заморожена.**

Первая полная версия архитектуры энциклопедии. Сформирован граф знаний, зафиксирована
модель данных, создана архитектурная документация.

### Добавлены сущности

| Сущность | Роль в графе |
|---|---|
| `Doctor` | Врач — центральный узел: клиники, заболевания, процедуры, оборудование, работы |
| `Clinic` | Клиника — врачи, оборудование, специализации, юр. данные |
| `Disease` | Заболевание |
| `Procedure` | Процедура / методика |
| `Equipment` | Оборудование |
| `EquipmentSpec` | Характеристика оборудования (группа/параметр/значение) |
| `ScientificWork` | Научная работа врача |
| `Publication` | Редакционный материал |
| `Supplier` | Поставщик оборудования |
| **Graph Knowledge Model** | 20 связующих таблиц, двусторонняя перелинковка |

Полный состав (39 моделей, включая справочники и служебные) — [`overview.md`](./overview.md) §2.

### Реализовано

- **Каталог врачей** — карточки, страница врача, связи с клиниками и энциклопедией
- **Каталог клиник** — фильтры, страница, блоки врачей и оборудования
- **Каталог оборудования** — страница с ТХ, галереей, документами и связями
- **Каталог публикаций** — объединяет прошедшие свои публичные фильтры
  `ScientificWork` и редакционные `Publication` без коллизий slug
- **Научная деятельность врача** — блок на странице врача + страница работы
- **`ScientificWork`** — научная работа как полноценная сущность со своим `slug`
- **`EquipmentSpec`** — характеристики любого прибора без изменения схемы
- **SEO-перелинковка** — canonical, OpenGraph, Schema.org, sitemap
- **Идемпотентный seed** — повторный прогон не создаёт дублей

---

## Замороженные модели

Изменение допускается **только после отдельного архитектурного решения**
с одновременным обновлением документации.

| Модель | Статус | Документ |
|---|---|---|
| **`Equipment`** (+ `EquipmentSpec`) | 🔒 **Заморожена** | [`equipment.md`](./equipment.md) |
| **`ScientificWork`** | 🔒 **Заморожена** | [`publications.md`](./publications.md) |
| **Graph Knowledge Model** | 🔒 **Базовая архитектура проекта** | [`graph-model.md`](./graph-model.md) |

### Что разрешено при замороженной модели

✅ Добавлять записи · связи · характеристики · фотографии · PDF · менять UI.

### Что запрещено

❌ Менять существующие поля · создавать альтернативные модели для покрытой области ·
менять семантику связей · менять структуру без архитектурного решения.

### Почему заморожены именно эти

- **`Equipment`** — масштабируемость подтверждена эмпирически: 8 разнотипных аппаратов
  (лазеры, ОКТ, биометры, микроскопы, витреосистемы) добавляются **только данными**.
  Ключ — `EquipmentCategory` + `EquipmentSpec` вместо колонок под каждый прибор.
- **`ScientificWork`** — покрывает диссертации, авторефераты, патенты, статьи,
  методические рекомендации, монографии. Тип работы — строка `type`, а не подмодель.
- **Graph Knowledge Model** — от неё зависят все страницы и вся перелинковка;
  изменение семантики связи ломает достоверность энциклопедии.

---

## Архитектурные решения v1

Полное изложение — [`design-principles.md`](./design-principles.md).

| Принцип | Суть |
|---|---|
| **Entity First** | Значимая единица знания — сущность со `slug` и страницей, а не поле или текст |
| **Graph Knowledge** | Ценность — в связях; каждая сущность встроена в граф, связи двусторонние |
| **SSOT** | У факта ровно один владелец; каталог читает таблицу, а не хранит копию |
| **Отказ от дублирования данных** | Никаких копий записей между таблицами и производных значений |
| **Только достоверные связи** | Нет источника — нет связи. Пустой раздел лучше выдумки |
| **Прямые связи предпочтительнее транзитивных** | Транзитивность — только если утверждение истинно по построению |
| **Расширение вместо дубликатов** | Сначала данные, потом связь, потом аддитивное поле; новая сущность — в последнюю очередь |
| **Аддитивность миграций** | Только `ADD COLUMN` (nullable) / `CREATE TABLE` / `CREATE INDEX`. Никаких `DROP` / `ALTER COLUMN` / `RENAME` |

### Ключевые решения, принятые в v1

**1. Связи научной работы — прямые, не через врача.**
Транзитивный вывод дал бы ложные медицинские связи: диссертация про кросслинкинг
попала бы на страницы «Катаракта» и «Птоз» (направления автора ≠ тема работы).
Созданы `ScientificWorkOnDisease` и `ScientificWorkOnProcedure`.
→ [`graph-model.md`](./graph-model.md) §4.2

**2. Клиники научной работы — через автора, без своей таблицы.**
`ScientificWorkOnClinic` **не создаётся**: факт уже выражен цепочкой
`work.doctor → DoctorOnClinic`. Здесь транзитивность корректна («клиники автора»).

**3. `Equipment → ScientificWork` не создаётся.**
Разные предметные области. Для исследований аппарата предусмотрена будущая сущность
`ClinicalStudy`, а не переиспользование `ScientificWork`.

**4. `Publication` и `ScientificWork` — разные сущности.**
У `ScientificWork` автор обязателен (владелец), у `Publication` — опционален.
Противоположные инварианты; объединение сломало бы гарантию «у работы есть автор».
Оба типа живут в одном каталоге `/publications`, различаясь полем `kind` **в типе
отображения**, а не в БД.
→ [`publications.md`](./publications.md)

**5. `EquipmentSpec` вместо колонок и `Json`.**
Характеристики приборов разных классов несопоставимы. Колонки росли бы под каждый
аппарат, `Json` непрозрачен. `group/label/value` — универсально и заморозило модель.

**6. Сущности `Organization` не существует.**
Её роли закрыты: `Supplier`, `Clinic` (юр. поля), `ScientificWork.organization`,
`Equipment.manufacturer`. Единый справочник юрлиц — отдельное решение.
→ [`overview.md`](./overview.md) §2.6

---

## Технический долг

Зафиксирован осознанно. **Не исправляется в рамках v1** — перечислен для будущих
архитектурных решений. Новых дублей вводить нельзя.

| Долг | Описание | Источник истины |
|---|---|---|
| `region` / `regionId` | У `Doctor` и `Clinic` регион хранится и строкой, и связью | `regionId` |
| `phone` / `phones` | У `Clinic` телефон хранится и строкой, и массивом | `phones` |
| `specializationTags` | `String[]` у `Clinic` при живой связи `ClinicOnSpecialty`. В схеме помечен как планируемый к миграции | `ClinicOnSpecialty` |
| nullable `ScientificWork.slug` | Технически вынужденно: `NOT NULL` на непустой таблице потребовал бы деструктивной миграции | Проставляется seed'ом; каталог фильтрует `slug != null` |
| Отсутствие квалификационной категории врача | «Высшую категорию» негде хранить: `Doctor.category` занята под группу фильтрации | — |
| Отсутствие отдельных «Сертификатов» / «Образования» | У врача только `career` единым текстом | — |
| Отсутствие счётчика отзывов | `prodoctorovUrl` хранит ссылку, но не число отзывов | — |

---

## Правила обновления

После каждого завершённого крупного этапа необходимо обновить:

1. **`ROADMAP.md`** — перенести задачу между блоками (⬜ → 🟡 → ✅), обновить фактические
   цифры наполнения.
2. **`CHANGELOG.md`** — добавить новую версию с описанием архитектурных изменений.

**Обязательно при изменении модели данных:**

3. Обновить профильный документ (`doctors.md`, `equipment.md`, …).
4. Обновить [`graph-model.md`](./graph-model.md), если появились/изменились связи —
   с указанием типа: прямая / вычисляемая / намеренно отсутствует.
5. Обновить [`overview.md`](./overview.md), если появилась новая сущность.

> Эти документы — **часть архитектуры проекта**, а не сопроводительный текст.
> Они являются источником истины наравне с `prisma/schema.prisma` и должны
> поддерживаться в актуальном состоянии. Изменение архитектуры без обновления
> документации считается незавершённым.

### Формат новой версии

```markdown
## vN — YYYY-MM-DD

**Статус:** …

### Добавлены сущности
### Реализовано
### Архитектурные решения
### Изменения замороженных моделей (если были — с обоснованием)
```
